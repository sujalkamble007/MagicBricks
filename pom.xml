<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.magicbricks</groupId>
	<artifactId>magicbricks-automation</artifactId>
	<version>1.0.0</version>
	<name>MagicBricks Automation Framework</name>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<!-- Java 11 Source and Target Compatibility -->
		<maven.compiler.source>11</maven.compiler.source>
		<maven.compiler.target>11</maven.compiler.target>
		
		<!-- Explicit, Non-floating Dependency Versions for Reproducible Builds -->
		<selenium.version>4.34.0</selenium.version>
		<testng.version>7.11.0</testng.version>
		<webdrivermanager.version>6.1.0</webdrivermanager.version>
		<extentreports.version>5.1.2</extentreports.version>
		<poi.version>5.2.5</poi.version>
		<commons.io.version>2.20.0</commons.io.version>
		<suiteXmlFile>testng.xml</suiteXmlFile>
	</properties>

	<dependencies>

		<!-- Selenium WebDriver: Core browser automation APIs, PageFactory, and Actions -->
		<dependency>
			<groupId>org.seleniumhq.selenium</groupId>
			<artifactId>selenium-java</artifactId>
			<version>${selenium.version}</version>
		</dependency>

		<!-- TestNG: Test execution engine, suite configurations, assertions, and data providers -->
		<dependency>
			<groupId>org.testng</groupId>
			<artifactId>testng</artifactId>
			<version>${testng.version}</version>
			<scope>test</scope>
		</dependency>

		<!-- WebDriverManager: Automated ChromeDriver binary management and browser resolution -->
		<dependency>
			<groupId>io.github.bonigarcia</groupId>
			<artifactId>webdrivermanager</artifactId>
			<version>${webdrivermanager.version}</version>
		</dependency>

		<!-- ExtentReports: Interactive HTML execution reporting with embedded failure screenshots -->
		<dependency>
			<groupId>com.aventstack</groupId>
			<artifactId>extentreports</artifactId>
			<version>${extentreports.version}</version>
		</dependency>

		<!-- Apache POI: Excel reader engine for Data-Driven Testing from TestData.xlsx -->
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi</artifactId>
			<version>${poi.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.poi</groupId>
			<artifactId>poi-ooxml</artifactId>
			<version>${poi.version}</version>
		</dependency>

		<!-- Apache Commons IO: File utilities for screenshot writing and directory operations -->
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
			<version>${commons.io.version}</version>
		</dependency>

		<!-- SLF4J Simple Logger: Provides clean console logging and silences SLF4J warning -->
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-simple</artifactId>
			<version>2.0.12</version>
			<scope>test</scope>
		</dependency>

		<!-- Log4j to SLF4J Bridge: Routes Apache POI logging cleanly to SLF4J and silences Log4j2 warning -->
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-to-slf4j</artifactId>
			<version>2.23.1</version>
			<scope>test</scope>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<!-- Maven Compiler Plugin: Enforces Java 11 compilation -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.11.0</version>
				<configuration>
					<source>11</source>
					<target>11</target>
				</configuration>
			</plugin>

			<!-- Maven Surefire Plugin: Executes TestNG suite (testng.xml) during mvn test -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>3.2.5</version>
				<configuration>
					<suiteXmlFiles>
						<suiteXmlFile>${suiteXmlFile}</suiteXmlFile>
					</suiteXmlFiles>
					<systemPropertyVariables>
						<webdriver.chrome.silentOutput>true</webdriver.chrome.silentOutput>
						<log4j2.StatusLogger.level>OFF</log4j2.StatusLogger.level>
						<java.util.logging.config.file>${project.basedir}/src/test/resources/logging.properties</java.util.logging.config.file>
					</systemPropertyVariables>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- Profile: Home and Login Modules Together -->
		<profile>
			<id>homelogin</id>
			<activation>
				<property>
					<name>homelogin</name>
				</property>
			</activation>
			<properties>
				<suiteXmlFile>testng-homelogin.xml</suiteXmlFile>
			</properties>
		</profile>

		<!-- Profile: Sell Module Separately -->
		<profile>
			<id>sell</id>
			<activation>
				<property>
					<name>sell</name>
				</property>
			</activation>
			<properties>
				<suiteXmlFile>testng-sell.xml</suiteXmlFile>
			</properties>
		</profile>
	</profiles>

</project>